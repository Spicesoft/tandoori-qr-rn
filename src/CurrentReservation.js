import React, {Component} from "react";
import {Container, Text} from "native-base";

export default class CurrentReservation extends React.Component {

  render() {
    const {reservation} = this.props.reservation;

    return (
        <Text>CurrentReservation pas implémenté (PD)</Text>
    );
  }

}
